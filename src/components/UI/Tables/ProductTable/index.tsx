import React from "react";
import { IProduct } from "shared/models";

interface IProps {
  products: IProduct[];
}

export const ProductTable = ({ products }: products) => {
  return <div>index</div>;
};

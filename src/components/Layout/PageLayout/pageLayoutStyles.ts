import { Style } from "shared/interfaces";

export const pageLayoutStyles: Style = {
  containerWrapper: {
    height: "100vh",
    display: "flex",
    flexDirection: "column",
  },
  container: {
    flex: 1,
  },
};

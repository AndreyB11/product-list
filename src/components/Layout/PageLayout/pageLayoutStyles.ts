import { IStyles } from "shared/interfaces";

export const pageLayoutStyles: IStyles = {
  containerWrapper: {
    height: "100vh",
    display: "flex",
    flexDirection: "column",
  },
  container: {
    flex: 1,
  },
};

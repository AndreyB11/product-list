import { Header } from "components/Layout/Header";
import { PageLayout } from "components/Layout/PageLayout";

const HomePage = () => {
  return <PageLayout header={<Header />}>Test</PageLayout>;
};

export default HomePage;
